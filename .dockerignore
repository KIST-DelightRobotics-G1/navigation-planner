# unitree_sdk2 is cloned (pinned) inside the image — don't COPY the host copy
# over the clone.
thirdparty/unitree_sdk2/

# parked (non-built) modules — keep them out of the build context / image.
_parked/

# build outputs / VCS
build/
.git/
